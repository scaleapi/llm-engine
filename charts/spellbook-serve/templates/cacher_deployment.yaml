apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "spellbookServe.cachername" . }}
  labels:
    {{- include "spellbookServe.selectorLabels.cacher" . | nindent 4 }}
    {{- include "spellbookServe.labels" . | nindent 4 }}
    tags.datadoghq.com/service: {{ include "spellbookServe.cachername" . }}
spec:
  replicas: {{ .Values.replicaCount.cacher }}
  selector:
    matchLabels:
      {{- include "spellbookServe.selectorLabels.cacher" . | nindent 6 }}
  template:
    metadata:
      annotations:
        ad.datadoghq.com/main.logs: |
          [{
            "service": {{ include "spellbookServe.cachername" . | quote }},
            "source": "python"
          }]
      labels:
        {{- include "spellbookServe.selectorLabels.cacher" . | nindent 8 }}
        {{- include "spellbookServe.labels" . | nindent 8 }}
        tags.datadoghq.com/service: {{ include "spellbookServe.cachername" . }}
        sidecar.istio.io/inject: "false"
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: {{ include "spellbookServe.cachername" . }}
          image: "{{ .Values.image.gatewayRepository }}:{{ .Values.tag}}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: 5000
              protocol: TCP
          readinessProbe:
            exec:
              command:
                - cat
                - /tmp/readyz
          command:
            - dumb-init
            - --
            - ddtrace-run
          args:
            - python
            - -m
            - spellbook_serve.entrypoints.k8s_cache
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
          {{- include "spellbookServe.cacherEnv" . | indent 10 }}
          {{- include "spellbookServe.volumeMounts" . | indent 10 }}
      serviceAccountName: {{ include "spellbookServe.fullname" . }}
      {{- include "spellbookServe.volumes" . | indent 6 }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
