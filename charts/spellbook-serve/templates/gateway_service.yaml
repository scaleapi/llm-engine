apiVersion: v1
kind: Service
metadata:
  name: {{ include "spellbookServe.fullname" . }}
  labels:
    {{- include "spellbookServe.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "spellbookServe.selectorLabels.gateway" . | nindent 4 }}
