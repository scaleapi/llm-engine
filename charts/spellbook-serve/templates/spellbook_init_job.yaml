{{- if .Values.secrets.kubernetesDatabaseSecretName }}
apiVersion: batch/v1
kind: Job
metadata:
  name: {{ include "spellbookServe.fullname" . }}-spellbook-setup
  labels:
    {{- include "spellbookServe.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": post-install
    "helm.sh/hook-weight": "0"
    "helm.sh/hook-delete-policy": hook-succeeded
spec:
  backoffLimit: 0
  activeDeadlineSeconds: 600
  template:
    metadata:
      labels:
        sidecar.istio.io/inject: "false"
        {{- include "spellbookServe.labels" . | nindent 8 }}
    spec:
      restartPolicy: Never
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: {{ include "spellbookServe.fullname" . }}
          image: "{{ .Values.image.gatewayRepository }}:{{ .Values.tag}}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          command:
            - dumb-init
            - --
          args:
            - python
            - -m
            - spellbook_serve.entrypoints.init_spellbook_models
            - --gateway-url
            - 'http://{{- include "spellbookServe.fullname" . }}.{{ .Release.Namespace }}:{{ .Values.service.port }}'
          {{- include "spellbookServe.serviceEnv" . | indent 10 }}
          {{- include "spellbookServe.volumeMounts" . | indent 10 }}
      serviceAccountName: {{ include "spellbookServe.fullname" . }}
      {{- include "spellbookServe.volumes" . | indent 6 }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
{{- end }}
