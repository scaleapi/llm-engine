{{- if .Values.config.values }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "spellbookServe.fullname" . }}-service-config
  labels:
    {{- include "spellbookServe.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": pre-install,pre-upgrade
    "helm.sh/hook-weight": "-2"
data:
  spellbook_serve_service_config: |-
    {{- with .Values.config.values.spellbook_serve }}
    {{- range $key, $value := . }}
    {{ $key }}: {{ $value | quote }}
    {{- end }}
    {{- end }}
  infra_service_config: |-
    env: {{ .Values.context | quote }}
    {{- with .Values.config.values.infra }}
    {{- range $key, $value := . }}
    {{ $key }}: {{ $value | quote }}
    {{- end }}
    {{- end }}
{{- end }}
